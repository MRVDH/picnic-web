// Icon bitmaps bundled with the web app, keyed by Picnic's icon key. The PNGs
// are the Picnic app's own drawables (xhdpi, 2x), copied from its APK, because
// Picnic's image CDN doesn't serve them.
export const ICON_ASSETS: Record<string, string> = {
  laurelLeafLeft: "/icons/app/laurelLeafLeft.png",
  laurelLeafRight: "/icons/app/laurelLeafRight.png",
};
