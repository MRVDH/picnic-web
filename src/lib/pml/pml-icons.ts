// PML ICON assets bundled with the web app, keyed by the PML `iconKey`.
// The PNGs are the Picnic app's own drawables (xhdpi, 2x), copied from its
// APK, because the ICON `fallback` images aren't served by the image CDN.
export const PML_ICON_SOURCES: Record<string, string> = {
  laurelLeafLeft: "/icons/pml/laurelLeafLeft.png",
  laurelLeafRight: "/icons/pml/laurelLeafRight.png",
};
