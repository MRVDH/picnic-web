/**
 * Geometry shared by Button and Chip so a row of them lines up exactly:
 * same height, radius, border width, shadow and text sizing. Each component
 * adds its own colours and padding on top.
 */
export const PILL_BASE_CLASSES =
  "inline-flex h-8 shrink-0 items-center rounded-full border text-sm font-medium shadow-sm transition-colors";
